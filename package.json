{
  "name": "zerkel",
  "version": "1.8.7",
  "description": "A compiler for the zerkel language",
  "scripts": {
    "clean-all": "npm run clean-cli && npm run clean-zerkel",
    "clean-cli": "cd packages/cli && npm run clean",
    "clean-zerkel": "cd packages/zerkel && npm run clean",
    "build-all": "npm run build-zerkel && npm run build-cli",
    "build-cli": "cd packages/cli && npm run build",
    "build-zerkel": "cd packages/zerkel && npm run build"
  },
  "workspaces": [
    "packages/zerkel",
    "packages/cli"
  ]
}
